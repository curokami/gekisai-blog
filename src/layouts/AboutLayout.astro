---
const { title, heroImage } = Astro.props;
---

<html lang="en">
  <head>
    <title>{title}</title>
  </head>
  <body>
    <main>
      {heroImage && <img src={heroImage} alt="About Image" />}
      <h1>{title}</h1>
      <slot />
    </main>
  </body>
</html>